import React from 'react';
import {shallow} from 'enzyme';
import {expect} from 'chai';
import App from './../assets/js/components/App';

describe('Component: App', () => {
    /*let wrapper;

    beforeEach(() => {
        wrapper = shallow(<App/>)
    });
    
    it('must be Rendered', () => {
        expect(wrapper.isEmpty()).to.be.equal(false);
    });*/
    
    it('myst be true', () => {
        expect(1).to.be.equal(1);
    });
        
        
});